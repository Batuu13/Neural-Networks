version: 3
automerge: false
projects:
- name: repository-manager
  dir: .
  workspace: default
  terraform_version: v1.3.0
  autoplan:
    enabled: true
    when_modified: ["./modules/**/*.tf", "*.tf*"]
